<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lawu.chick.repository.mapper.extend.UserRelationDOMapperExtend" >
    <resultMap id="BaseResultMap" type="com.lawu.chick.repository.domain.extend.FriendDOView" >
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <result column="friend_num" property="friendNum" jdbcType="VARCHAR" />
        <result column="nickname" property="nickName" jdbcType="VARCHAR" />
        <result column="gender" property="gender" jdbcType="VARCHAR" />
        <result column="avatarUrl" property="avatarUrl" jdbcType="INTEGER" />
        <result column="eggs" property="eggs" jdbcType="DECIMAL" />
    </resultMap>

    <select resultMap="BaseResultMap" parameterType="com.lawu.chick.repository.param.FriendParam" id="getFriendList" >
      SELECT
        r.friend_num,
        u.nickname,
        u.gender,
        u.avatarUrl,
        m.eggs
    FROM
        user_relation AS r
    INNER JOIN wx_user AS u ON r.friend_num = u.member_num
    INNER JOIN member as m ON r.friend_num = m.num
    WHERE
        r.member_num = #{memberNum}
    order by m.eggs desc,r.id desc
    limit #{offset},#{pageSize}
    </select>

    <select resultType="java.lang.Integer" parameterType="com.lawu.chick.repository.param.FriendParam" id="getFriendListCount" >
        SELECT
        count(r.id)
        FROM
        user_relation AS r
        INNER JOIN wx_user AS u ON r.friend_num = u.member_num
        INNER JOIN member as m ON r.friend_num = m.num
        WHERE
        r.member_num = #{memberNum}

    </select>

</mapper>